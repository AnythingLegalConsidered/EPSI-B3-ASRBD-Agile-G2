# ============================================================
# WARNING: These are EXAMPLE values only!
# Do NOT use these in production.
# Replace with real secrets before deploying.
# ============================================================
#
# To encode your own values:
#   echo -n 'your-secret-value' | base64
#
# To decode:
#   echo 'base64-string' | base64 -d
# ============================================================
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: prod
  labels:
    app: backend
type: Opaque
data:
  # EXAMPLE: "changeme123" in base64
  DB_PASSWORD: Y2hhbmdlbWUxMjM=
  # EXAMPLE: "symfony-app-secret-key-change-me" in base64
  APP_SECRET: c3ltZm9ueS1hcHAtc2VjcmV0LWtleS1jaGFuZ2UtbWU=
